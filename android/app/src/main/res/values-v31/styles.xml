<?xml version="1.0" encoding="utf-8"?><resources>


<style name="Theme.YallaGo" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Android 12+ splash: make it blank and instant -->
    <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
    <item name="android:windowSplashScreenAnimatedIcon">@null</item>
    <item name="android:windowSplashScreenIconBackgroundColor">@null</item>
    <item name="android:windowSplashScreenAnimationDuration">0</item>

    <!-- Background behind Flutter after first frame -->
    <item name="android:windowBackground">@color/splash_bg</item>
</style>

<style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
    <item name="android:windowBackground">@color/splash_bg</item>
</style>
</resources>